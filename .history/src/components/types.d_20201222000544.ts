type PlayNumberProps = {
  number: number;
  status: string;
  onClick:function name(params:type) {
    
  }
};

type colorsType = {
  [key: string]: string;
};

type StarsDisplayProps = {
  stars: number;
};
