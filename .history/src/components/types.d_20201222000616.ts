type PlayNumberProps = {
  number: number;
  status: string;
  onClick:function(ev:React.MouseEvent) {
    
  }
};

type colorsType = {
  [key: string]: string;
};

type StarsDisplayProps = {
  stars: number;
};
